(function () {
    'use strict';

    angular.module('ep.axis.offerPortal', [
        'ep.axis.core',
        'formly',
        'LocalStorageModule'
    ]).run(['localStorageService', '$rootScope', function (localStorageService, $rootScope) {
    }]);

})();
